# 🔧 اصلاحات ۱۲ اوت ۲۰۲۶ (P0 + P1 + P2)

این دوره بر اساس ممیزی کامل (مرورگر واقعی + خواندن کد + تست API روی سایت زنده) انجام شد.
همه تغییرات با `npm run verify` (typecheck ✅ lint ✅ data ✅ 40 تست ✅) و بیلد نهایی ✅ تأیید شده‌اند
و جریان‌های کلیدی با مرورگر هدلس روی بیلد پروداکشن محلی smoke-test شده‌اند.

---

## 🔴 P0 — امنیت و پول

### 1) لاگین با هر رمزی ممکن بود → حذف نشست جعلی
`src/app/[locale]/login/LoginClient.tsx`
- قبلاً هر خطای Supabase (رمز اشتباه!) به ساختنشست `localStorage` با ایمیل خام منتهی می‌شد — یعنی ورود با هر ایمیل/رمزی.
- الان: رمز اشتباه → نمایش خطای واقعی «ایمیل یا رمز عبور اشتباه است». ایجاد نشست محلی فقط وقتی Supabase اصلاً کانفیگ نشده (dev لوکال).
- خطای signUp و magic-link هم واقعی نمایش داده می‌شود (کلیدهای ترجمه جدید در هر ۸ زبان: `invalidCredentials`, `authErrorGeneric`, `magicLinkError`).

### 2) وبهوک Plisio بدون امضا → تأیید سه‌لایه، fail-closed
`src/app/api/checkout/plisio-webhook/route.ts` + `src/lib/plisio.ts`
- قبلاً هر POST با `status: completed` سفارش را confirm می‌کرد (پولی!)
- الان: ① امضای `verify_hash` (HMAC-SHA1 با کلید پلیسیو) اجباری — بدون امضای معتبر `403`؛
  ② تأیید سرور‌به‌سرور از API خود پلیسیو (`operations`)؛
  ③ چک مبلغ پرداخت‌شده ≥ مبلغ سفارش؛ `mismatch` دیگر خودکار confirm نمی‌شود (رفتار قبلی باگ بود).
- اگر lookup پلیسیو موقت fail شود → سفارش pending می‌ماند برای بازبینی دستی (نه confirm کور، نه reject اشتباه).

### 3) 🔑 کلید API پلیسیو در کد هاردکد بود → حذف کامل
`src/lib/plisio.ts` — fallback هاردکد حذف شد؛ بدون `PLISIO_API_KEY` چک‌اوت fail-closed می‌شود.
⚠️ **اجباری: کلید را در داشبورد پلیسیو Rotate کن (کلید قدیمی در گیت‌هاب پابلیک لو رفته است).**

### 4) دور زدن سهمیه AI Studio → سهمیه سروری دائمی
`src/lib/quotaStore.ts` + `src/app/api/ai-studio/generate/route.ts` + `supabase/migrations/0017_ai_usage_daily.sql`
- قبلاً سهمیه با فیلد `email` از بدنه درخواست — یعنی بی‌نهایت تولید رایگان با ایمیل ساختگی. و استور هم in-memory بود (با هر cold start صفر).
- الان: هویت فقط سمت سرور از توکن Supabase خوانده می‌شود؛ کاربر مهمان → سهمیه IP. استور روی جدول `ai_usage_daily` دائمی است (Supabase) با fallback حافظه برای dev.
- کلاینت‌های استودیو (`studioAuthClient.ts`) توکن نشست را به هدر Authorization می‌فرستند.
- **یادآوری: مایگریشن 0017 را در SQL Editor سوپابیس اجرا کن.**

### 5) API خلاصه حساب عمومی بود → فقط با نشست
`src/app/api/account/summary/route.ts` — پارامتر `?email=` حذف شد؛ ایمیل فقط از توکن معتبر. صفحه اکانت با هدر Bearer صدا می‌زند. بدون نشست: `401`.
- بادج‌اسنیپت‌ها هم به `SITE_URL` مرکزی وصل شدند (دیگر دامنه هاردکد نیست).

### 6) لینک تأیید خبرنامه در پاسخ API لو می‌رفت
`src/app/api/newsletter/route.ts` — `devConfirmUrl` فقط در dev برمی‌گردد.

---

## 🟠 P1 — SEO حیاتی و ویدیوها

### 7) CSP ویدیوهای دمو و یوتیوب را بلاک می‌کرد
`next.config.js` — اضافه شد: `media-src 'self' https://*.supabase.co` و `frame-src https://www.youtube.com https://www.youtube-nocookie.com` + `img.youtube.com/i.ytimg.com` در img-src.
قبلاً همه ویدیوهای دمو و امبد یوتیوب در صفحه اصلی/ابزارها بی‌صدا خراب بودند.

### 8) canonical دامنه قدیمی → مستندات دپلوی (پایین همین فایل)
کد درست است؛ مشکل env ورسل توست → بخش «کارهای خودت» را بخوان.

---

## 🟡 P2 — تجربه کاربری

### 9) `/fa` نیمه‌انگلیسی → اوررایدهای محتوا per-locale
`src/lib/siteContent.ts` — اوررایدهای انگلیسی ادمین دیگر روی زبان‌های دیگر تحمیل نمی‌شود. برای هر زبان می‌توان `content_fa_...` جدا در `site_settings` ذخیره کرد. تأیید شد: هیروی `/fa` حالا کاملاً فارسی است.

### 10) دکمه `studio.tt.copy` (کلید ترجمه به‌جای متن) → اضافه شد به ۸ زبان
+ رشته‌های هاردکد انگلیسی ThumbnailTextGenerator هم به دیکشنری منتقل شد (analyzing/resultHeading/engineLabel/…).

### 11) خروجی Markdown خام AI → رندر امن
`src/components/studio/MarkdownLite.tsx` (جدید، بدون dependency، بدون XSS — React nodes، نه innerHTML) برای ThumbnailText و ThumbnailBrief. خروجی الان تیتر/بولد/لیست/کد قشنگ دارد.
(PromptBuilder عمداً monospace ماند چون خروجی‌اش برای کپی در میدجرنی است.)

### 12) باگ لایت‌مود استودیو → CSS overrides
`studio.css` — سلکت‌ها نامرئی بودند (متن سفید روی کارت روشن)، خروجی AI و دکمه‌های کوچک هم در لایت‌مود فیکس شدند.

### 13) «0 tested hands-on» در فوتر → نسخه نرم
`Footer.tsx` + کلید `footer.taglineBlurbNoTest` در ۸ زبان. وقتی اولین تست‌ها را زدی خودکار به متن کامل برمی‌گردد.

### 14) `Back to All Tools` هاردکد → `t('backToTools')` (کلید موجود بود، استفاده نشده بود!)

### 15) چینمان ستون‌های Compare → باگ کلاس داینامیک Tailwind
`CompareClient.tsx` — `sm:grid-cols-${cols}` هیچ‌وقت کامپایل نمی‌شد؛ کارت‌ها روی دسکتاپ زیر هم بودند. فیکس شد (`sm:grid-cols-2/3`).

### 16) چسبیدگی متن H1ها → فاصلهٔ واقعی در DOM
صفحه اصلی، AI Studio و Guide: «AI toolwithout» → «AI tool without» (برای اسکرین‌ریدر و گوگل).

### 17) کنتراست سوییچر زبان در تم روشن → فیکس شد.

---

---

## 🆕 بخش دوم — بازبینی لاگین + تست دوباره درگاه با مرورگر واقعی (عصر ۱۲ اوت)

این دوره بعد از گزارش باگی است که خودت روی `/fa/login` دیدی. همه جریان‌ها با **مرورگر واقعی Chromium روی بیلد پروداکشن** و با بک‌اند زنده Supabase تست شدند (اسکرین‌شات‌ها در پوشه تست‌ها: `livetest/shots/`).

### ۱۸) 🐛 باگ گزارش‌شده‌ات: «ارسال ایمیل الکی است + بعد می‌گه رمز اشتباهه» → ریشه‌یابی و فیکس کامل
`src/app/[locale]/login/LoginClient.tsx`
- **علت واقعی:** دو باگ روی هم سوار شده بود:
  1. بعد از ثبت‌نام، دکمه «Continue to Dashboard» یک **سشن تقلبی** لوکال می‌ساخت و می‌بردت اکانت — در حالی که در واقعیت هیچ سشنی وجود نداشت (چون ایمیل تأیید نشده بود). برای همین حس می‌کردی «الکیه».
  2. وقتی بعداً رمز درست را می‌زدی، Supabase خطای `email_not_confirmed` برمی‌گرداند ولی UI آن را به‌اشتباه «رمز اشتباه» نشان می‌داد.
- **الان (تأییدشده با تست واقعی ۱۲/۱۲ ✅ مرحله):** بعد از ثبت‌نام پیام صادقانه نمایش داده می‌شود («لینک تأیید به ایمیل تو ارسال شد») + دکمه «برو به ورود». ورود قبل از تأیید → **باکس کهربایی** «ایمیلت هنوز تأیید نشده» (با آدرس ایمیل واقعی داخل متن) + لینک «ارسال دوباره ایمیل تأیید». رمز واقعاً اشتباه → همان ارور قرمز قبلی. 
- `emailRedirectTo` به signUp و resend اضافه شد — قبلاً آن را نمی‌فرستادیم و لینک تأیید ممکن بود به Site URL پیش‌فرض سوپابیس (localhost!) برگردد. ⚠️ **در Supabase به بخش URL Configuration برو (پایین صفحه YOUR-TASKS توضیح داده‌ام).**

### ۱۹) ارورهای خام انگلیسی Supabase → پیام‌های فارسی/محلی
کدهای رایج (`over_email_send_rate_limit`, `email_address_invalid`, `weak_password`, `user_already_exists`) الان پیام ترجمه‌شده در هر ۸ زبان دارند. (در حین تست به‌چشم دیدم که «email rate limit exceeded» خام به کاربر ایرانی نشان داده می‌شد.)

### ۲۰) ترتیب تب‌های ورود برای RTL مرتب شد
در دسکتاپ فارسی تب «ورود» سمت راستِ «ثبت‌نام» بود؛ الان ترتیب بصری درست است (ثبت‌نام چپ، ورود راست فعال).

### ۲۱) باگ پنهان ولی جدی: ترکیب اشتباه `t.rich` + پیام‌های `{placeholder}` 
React این الگو را با «Functions are not valid as a React child» رد می‌کند — و در رندر واقعی **متن/لینک‌های داخل این جمله‌ها خالی چاپ می‌شدند**. ۴ ناحیه پیدا و فیکس شد:
- **Login** «با ادامه دادن، با [قوانین] و [حریم خصوصی]…» → لینک‌ها قبلاً **خالی** بودند (!)؛ پیام‌ها در هر ۸ زبان به سینتکس تگ `<terms>…</terms>` تغییر کرد و الان لینک‌ها واقعاً رندر می‌شوند (با curl روی بیلد پروداکشن اثبات شد).
- **Submit** پیام موفقیت → قبلاً `{name}` و `{email}` خالی رندر می‌شدند؛ الان نام ابزار و ایمیل واقعی جایگذاری می‌شود (تست مرورگری ۴/۴ ✅).
- **Founders** صفحه Claim → همان فیکس.
- **Admin** (داخلی) — ۶ پیام با `{newsQueue}/{pending}/…` به فرمت تگ تبدیل شدند (۷۲ جایگذاری در ۸ فایل زبان).

### ۲۲) نتیجه تست دوباره درگاه پرداخت (خودم انجام دادم 🧾) — بدون انتقال واقعی پول، ولی کامل
روی بیلد پروداکشن با مرورگر واقعی، جریان Submit Tool → پلن ۲۹$ → چک‌اوت را کامل گشتم: **۹/۹ ✅**
- سفارش ساخته می‌شود (`ord_…`)، بدون کلید پلیسیو به‌جای کرش، به‌تمیزی روی **حالت دستی** می‌نشیند: آدرس کیف پول TRC20 + QR + دکمه کپی + فرم TXID.
- TXID جعلی (`deadbeef…`) زدم → سفارش فقط به وضعیت `submitted` (صف بازبینی دستی) رفت، **نه** `paid`. یعنی هیچ‌کس با TXID ساختگی نمی‌تواند اشتراک بگیرد.
- تایید خودکار فقط از مسیر وب‌هوک پلیسیو + امضای HMAC + چک سرور‌به‌سرور مبلغ است (۹ تست واحد ✅ + شبیه‌سازی زنده: fake → `403`، امضا-درست/نامعلوم → `202 pending`).
- ریت‌لیمیت وبه‌هوک و verify هم فعال است.

### ۲۳) یافته مهم درباره ایمیل Supabase (برای لانچ حیاتی) ⚠️
SMTP پیش‌فرض Supabase **سهمیه خیلی کوچکی** دارد (چند ایمیل در ساعت). در تست‌های امروز بارها به `over_email_send_rate_limit` خوردیم. برای لانچ واقعی حتماً **SMTP اختصاصی** وصل کن — راهنمای Resend (رایگان تا ۱۰۰ ایمیل/روز) در `YOUR-TASKS-FA.md` اضافه شده. وگرنه کاربران واقعی در ثبت‌نام به خطای ریت‌لیمیت می‌خورند (البته الان حداقل پیام فارسی می‌بینند!).

### وضعیت تأیید فنی بخش دوم
`typecheck ✅` · `lint ✅` · `49/49 تست ✅` · بیلد پروداکشن (نسخه smoke ترِجیحی کم‌حافظه برای سندباکس ۲گیگی — همه روت‌ها کامپایل شدند) ✅ · جریان‌ها با Chromium واقعی ✅

---

## 📋 کارهای خودت (نمی‌توانم از اینجا انجامشان بدهم)

1. **Vercel → Environment Variables:**
   - `NEXT_PUBLIC_SITE_URL=https://creatorsaicenter.vercel.app` (الان canonical/sitemap به دامنه قدیمی `directory-ai-hub` اشاره می‌کند — بعد از برداشتن سایت روی دامنه اختصاصی، همین را به دامنه جدید عوض کن)
   - `PLISIO_API_KEY=کلید-جدید-بعد-از-rotate`
   - اگر نداری: `ADMIN_SESSION_SECRET=openssl rand -hex 32`
   - اختیاری ولی توصیه: `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` (ریت‌لیمیت واقعی جهانی — رایگان تا ۱۰هزار کامند/روز)
2. **Supabase → SQL Editor:** اجرای `supabase/migrations/0017_ai_usage_daily.sql`
3. **Plisio Dashboard:** Rotate کردن کلید لو رفته + در تنظیمات فروشگاه callback URL را روی `/api/checkout/plisio-webhook` بگذار و **یک پرداخت تستی ۱ دلاری انجام بده** تا زنجیره امضا↔confirm را کامل دیده باشی (اگر امضا match نشد لاگ Vercel را بفرست — کاندیدای الگوریتم را دقیقش می‌کنم).
4. **پاک‌سازی موارد تست من در پنل ادمین:** ریویوی pending «AuditBot» روی OpusClip + ایمیل `probe-audit-123@example.com` در خبرنامه.
5. **دامنه قدیمی:** در ورسل روی پروژه/دامنهٔ قدیمی `domain redirect` 301 به دامنه جدید بگذار (Settings → Domains).

## سمت چپ گزارش ممیزی (عمداً انجام نشده، نیازمند تصمیم تو)

- کپچا (Cloudflare Turnstile — رایگان) روی submit/checkout — بعد از لانچ اولیه.
- سبک‌سازی صفحه اصلی (۳.۱ مگ): dynamic import برای صحنه‌های سه‌بعدی — فاز بعدی.
- ریپوی پابلیک: در برند README هست؛ در نظر بگیر private شود.
- ترجمه‌کامل هاردکدهای دیگر کامپوننت‌های استودیو (پروژه جدا — تعداد زیاد).
