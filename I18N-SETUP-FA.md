# 🌐 سیستم چندزبانه و ترجمه حرفه‌ای CreatorAI Hub

> راهنمای راه‌اندازی و نحوه کار سیستم i18n — نسخه ۱ (مرداد ۱۴۰۵ / آگوست ۲۰۲۶)

این سیستم سایت را به **۸ زبان** می‌برد: انگلیسی (پیش‌فرض)، اسپانیایی، پرتغالی، فرانسوی، آلمانی، چینی ساده‌شده، عربی و فارسی — بدون تغییر در طراحی.

---

## ۱) زبان‌ها و آدرس‌ها

| زبان | پیشوند | جهت |
|---|---|---|
| English | `/` (بدون پیشوند) | چپ‌به‌راست |
| Español | `/es` | چپ‌به‌راست |
| Português | `/pt` | چپ‌به‌راست |
| Français | `/fr` | چپ‌به‌راست |
| Deutsch | `/de` | چپ‌به‌راست |
| 简体中文 | `/zh` | چپ‌به‌راست |
| العربية | `/ar` | **راست‌به‌چپ** |
| فارسی | `/fa` | **راست‌به‌چپ** |

- سوییچر زبان در هدر همه صفحات است (آیکون 🌐). با تغییر زبان، در همان صفحه می‌مانید (مثلاً `/fa/tool/opusclip` ↔ `/es/tool/opusclip`).
- بازدیدکننده‌ای که زبان مرورش با یکی از این ۸ زبان باشد، به‌صورت خودکار همان زبان را پیشنهاد می‌گیرد.
- برای سئو: برچسب `hreflang` برای هر ۸ زبان در هدر HTTP همه صفحات ارسال می‌شود.

## ۲) فونت‌های قوی

هر زبان فونت بومی مخصوص خودش را دارد (self-hosted، بدون درخواست خارجی):

- **لاتین (en/es/pt/fr/de):** Inter (فونت قبلی سایت)
- **فارسی:** Vazirmatn — مرجع فونت مدرن فارسی
- **عربی:** Noto Sans Arabic
- **چینی:** Noto Sans SC

فونت‌ها فقط وقتی دانلود می‌شوند که متن آن زبان واقعاً در صفحه باشد (unicode-range)، پس سرعت صفحات انگلیسی دست‌نخورده می‌ماند. جهت راست‌به‌چپ هم برای فارسی و عربی به‌صورت خودکار اعمال می‌شود (`dir="rtl"`).

## ۳) موتور ترجمه حرفه‌ای (نه گوگل‌ترنسلیت!)

ترجمه‌ها با یک **موتور ترجمه مبتنی بر LLM** تولید می‌شوند که مثل یک مترجم حرفه‌ای انسانی کار می‌کند:

- ✅ نام ابزارها، برندها، URLها و اعداد را **هرگز** ترجمه نمی‌کند
- ✅ لحن و منظور متن را حفظ می‌کند (متن بازاریابی، روان و طبیعی است، نه ترجمه لغت‌به‌لغت)
- ✅ از یک **گلاسری دامنه‌ای** استفاده می‌کند (`src/lib/i18n/glossary.ts` — مثلاً «thumbnail» در هر زبان ترجمه ثابت خودش را دارد)
- ✅ با یک تماس API چند فیلد را با هم ترجمه می‌کند (کم‌هزینه)
- ✅ **پشتیبانی از فارسی** دارد (که دیپ‌ال ندارد!)

### راه‌اندازی موتور — سه گزینه (یکی را انتخاب کن)

**گزینه ۱ — Google Gemini (رایگان ✅ بهترین انتخاب)**
```bash
# کلید رایگان: https://aistudio.google.com → Get API key (بدون کارت اعتباری)
GEMINI_API_KEY=...
# سهمیه رایگان: چندصد درخواست در روز (کافی برای یک زبان در روز)
```

**گزینه ۲ — OpenRouter (رایگان ✅)**
```bash
# کلید رایگان: https://openrouter.ai (بدون نیاز به اعتبار برای مدل‌های :free)
OPENROUTER_API_KEY=...
OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free
# سهمیه رایگان ~۵۰ درخواست در روز → با --batch 8 اجرا کن
```

**گزینه ۳ — OpenAI (پولی، ارزان)**
```bash
OPENAI_API_KEY=sk-...            # نیاز به شارژ حساب دارد (حتی ۵ دلار کافی)
TRANSLATION_AI_MODEL=gpt-4o-mini # پیش‌فرض — کل کار ≈ ۲ تا ۳ دلار
```

اگر چند کلید داری، با `TRANSLATION_PROVIDER=gemini|openrouter|openai` یکی را صریح کن؛ وگرنه خودکار تشخیص داده می‌شود (OpenAI اول، بعد Gemini، بعد OpenRouter).

**نکته سهمیه رایگان:** با Gemini/OpenRouter از `--batch` استفاده کن تا چند موجودیت در هر تماس ترجمه شوند و کل کار در سهمیه روزانه جا شود:
```bash
npm run translate:content -- --limit 300 --batch 4 --concurrency 2   # برای Gemini
npm run translate:content -- --limit 50  --batch 8                    # برای OpenRouter
```
اجرا **قابل ادامه** است: اگر سهمیه روز تمام شود، فردا دوباره همان دستور را بزن — فقط موارد باقی‌مانده ترجمه می‌شوند.

## ۴) ترجمه خودکار محتوای جدید

سه لایه «خودکار» داریم:

### الف) کرون روزانه Vercel
`/api/cron/translate` هر روز اجرا می‌شود (به‌همراه `CRON_SECRET`). هر بار تا ۴۰ ابزار/مطلب در هر زبان را که ترجمه ندارند یا متن انگلیسی‌شان تغییر کرده (تشخیص با هش محتوا) ترجمه می‌کند.

### ب) GitHub Action (مطمئن‌ترین راه — حتی روی پلن رایگان Vercel)
فایل `.github/workflows/translate.yml` هر روز ساعت ۰۳:۱۷ UTC اجرا می‌شود:
- همان همگام‌سازی را اجرا می‌کند
- **فایل‌های snapshot ترجمه را کامیت می‌کند** (`src/i18n-content/*.json`) تا سایت حتی بدون دیتابیس هم محتوا را ترجمه‌شده نشان دهد
- یک Pull Request باز می‌کند تا شما سریع مرور و merge کنید

فقط کافی است در GitHub → Settings → Secrets این‌ها را اضافه کنید:
```
OPENAI_API_KEY
SUPABASE_URL            (اختیاری — برای ذخیره در دیتابیس)
SUPABASE_SERVICE_ROLE_KEY  (اختیاری)
```

### ج) ترجمه فوری اخبار
وقتی کرون خبر جدیدی از RSS می‌گیرد، بلافاصله همان لحظه ترجمه می‌شود (در `/api/news/refresh`).

### د) دستورهای دستی

```bash
npm run translate:status               # وضعیت پوشش هر زبان
npm run translate:content              # ترجمه همه محتوای در انتظار (DB + snapshot)
npm run translate:content -- --locales fa,es --limit 20 --dry-run
npm run translate:ui                   # همگام‌سازی دیکشنری‌های UI از en.json
```

### ذخیره‌سازی

ترجمه‌ها در جدول Supabase `content_translations` ذخیره می‌شوند (مایگریشن `supabase/migrations/0013_i18n_translations.sql`). اگر Supabase تنظیم نباشد، از فایل‌های snapshot داخل ریپو خوانده می‌شود و اگر آن هم نباشد، انگلیسی (همیشه به‌عنوان fallback).

## ۵) دیکشنری‌های رابط کاربری (UI)

متن‌های ثابت رابط کاربری (منوها، دکمه‌ها، تیترها) در `messages/{locale}.json` هستند — برای هر ۸ زبان **به‌صورت حرفه‌ای و دستی ترجمه شده‌اند** (نه ماشینی). اگر متن جدیدی به یک صفحه اضافه کردید:

1. کلید را به `messages/en.json` اضافه کنید
2. `npm run translate:ui` را بزنید تا بقیه زبان‌ها با موتور ترجمه تولید شوند
3. یا `node scripts/check-keys.mjs` بزنید تا مطمئن شوید هیچ کلیدی جا نیفتاده

## ۶) تبدیل صفحات جدید به چندزبانه (الگو)

هر صفحه جدید را با این ۳ قدم چندزبانه کنید:

```tsx
// 1) لوکال را از params بگیر
export default async function Page({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  setRequestLocale(locale);
  // 2) ترجمه UI
  const t = await getTranslations({ locale, namespace: 'myPage' });
  // 3) محتوای ابزارها/اخبار را لوکالایز کن
  const tools = await localizeTools(ALL_TOOLS, locale);
```

## ۷) هزینه‌ها (تقریباً)

- ترجمه کامل کاتالوگ (~۵۰۰ ابزار × ۷ زبان): حدود **۲ تا ۵ دلار** با `gpt-4o-mini`
- کرون روزانه فقط موارد جدید/تغییریافته را ترجمه می‌کند → تقریباً رایگان
- برای صفر نگه‌داشتن هزینه در روزهای اول، `--limit` کمتر بدهید

---

**سوال؟** این فایل را به فارسی نوشته‌ایم تا خواندنش راحت باشد؛ منطق سیستم در `src/lib/i18n/` با کامنت انگلیسی مستند شده است.

---

## ۸) رفع مشکل E2E (Timed out waiting from config.webServer)

اگر `npx playwright test` با خطای **`Timed out waiting 120000ms from config.webServer`** مواجه شد، علت ریشهیابی شده و در کد نهایی برطرف شده است:

**علت:** با `localePrefix: 'as-needed'`، میدلور `/` را به `/en` رورایت (rewrite) میکند تا سگمنت `[locale]` مقدار بگیرد. اما matcher میدلور مسیر رورایتشدهی `/en` را هم match میکرد → Next میدلور را دوباره روی `/en` اجرا میکرد → حالت `as-needed` آدرس پیشونددار زبان پیشفرض را غیرکنونیکال میداند و به `/` ریدایرکت 307 میداد → **لوپ بینهایت `/` ⇄ `/en`** → health check پلیرایت (که 200/3xx میخواهد) هیچوقت موفق نمیشد.

**راهحل (پیادهشده):** در `src/middleware.ts` مقدار `en` به لیست حذفشدههای matcher اضافه شده:

```ts
matcher: ['/((?!api|_next|_vercel|.*\\..*|en|badge|go|feed\\.xml|feed-tools\\.xml|llms\\.txt|robots\\.txt|sitemap\\.xml|manifest\\.webmanifest).*)']
```

حالا `/` با 200 (بدون هیچ ریدایرکتی) سرو میشود، health check فوراً پاس میشود و `Accept-Language` برای زبانهای غیرانگلیسی همچنان ریدایرکت خودکار دارد.

**نکته دوم:** اگر صفحهای کلاینتکامپوننت از کامپوننتهای سرویسکامپوننت (مثل Footer با `getTranslations`) import کند، خطای `getTranslations is not supported in Client Components` میگیرد. فوتتر به کلاینتکامپوننت تبدیل شده (`useTranslations`) تا صفحات کلاینت (Compare, Stack Builder, Login, Account) سالم بمانند.

---

## ۹) محتوای از پیش ترجمهشده (Seed Content) — حالا همه زبانها کاملاند

دیگر لازم نیست منتظر کلید OpenAI بمانید: **کل محتوای کاتالوگ از قبل به هر ۷ زبان ترجمه و داخل ریپو کامیت شده است.**

| زبان | ابزارها (۲۰۶) | دستهبندیها (۱۷) | اخبار (۶۵) | مقالات (۱۰) |
|---|---|---|---|---|
| فارسی /fa | ✅ | ✅ | ✅ | ✅ |
| اسپانیایی /es | ✅ | ✅ | ✅ | ✅ |
| پرتغالی /pt | ✅ | ✅ | ✅ | ✅ |
| فرانسوی /fr | ✅ | ✅ | ⏳ | ⏳ |
| آلمانی /de | ✅ | ✅ | ⏳ | ⏳ |
| چینی /zh | ✅ | ✅ | ⏳ | ⏳ |
| عربی /ar | ✅ | ✅ | ⏳ | ⏳ |

- ترجمهها در `scripts/seed-data/{locale}.*.json` (فایلهای منبع) و `src/i18n-content/{locale}.json` (خروجی) ذخیره شدهاند.
- بازتولید: `node scripts/build-snapshot.mjs fa es pt fr de zh ar`
- هر چیزی که بعداً اضافه شود (ابزار جدید، خبر جدید، مقاله جدید) توسط موتور ترجمه خودکار (`npm run translate:content`) یا با افزودن به فایلهای seed پر میشود.
- برندها، اعداد و اصطلاحات فنی مثل «4K» عمداً در همه زبانها یکسان ماندهاند (ترجمه نشدهاند) — این درست است.


---

## ۱۰) گذاشتن کلید در پنل ادمین (بدون دست زدن به env)

کلید ترجمه را میتوانی مستقیم از **پنل ادمین** (`/admin` ← تب **Translation**) وارد کنی — بدون نیاز به تنظیم env در Vercel:

1. برو به `/admin` و وارد شو
2. تب **Translation** را باز کن
3. Provider را انتخاب کن (Gemini رایگان / OpenRouter رایگان / OpenAI)
4. کلید را بچسبان و **Save keys** را بزن
5. **Test connection** را بزن تا مطمئن شوی کلید کار میکند (یک ترجمه آزمایشی فارسی میزند)

**امنیت:**
- کلیدها در جدول `site_settings` (کلیدهای `i18n_*`) ذخیره میشوند
- فقط سمت سرور خوانده میشوند؛ **هرگز کامل نمایش داده نمیشوند** (فقط ۴ حرف آخر)
- endpoint عمومی `/api/settings` آنها را برنمیگرداند — فقط route ادمین
- اگر env هم ست شده باشد، env اولویت دارد

**نحوه کار بعد از ذخیره:**
- کرون روزانه `/api/cron/translate` خودش از دیتابیس میخواند → **محتوای جدید خودکار ترجمه میشود** (بدون هیچ کار اضافه)
- برای ترجمه یکجا (backfill) متنهای بلند: GitHub → Actions → **Auto-Translate Content** → Run workflow → limit `500`
  (این Action به `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` در GitHub Secrets نیاز دارد تا کلید دیتابیس را ببیند؛ یا همان دستور را محلی اجرا کن.)

## ۱۱) دستور ترجمه را کجا اجرا کنم؟

سه راه، از آسانترین:

1. **GitHub Actions (بهترین — بدون نیاز به کامپیوتر خودت):**
   GitHub → ریپو → **Actions** → **Auto-Translate Content** → **Run workflow** → limit را `500` بگذار → Run.
   هر روز هم خودکار اجرا میشود. (نیاز: `OPENAI_API_KEY` یا `GEMINI_API_KEY` یا `OPENROUTER_API_KEY` + `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` در **GitHub → Settings → Secrets** — یا اگر کلید را در پنل ادمین گذاشتهای، فقط دو متغیر Supabase کافی است.)

2. **محلی (روی کامپیوتر خودت):**
   ```
   cd <پوشه ریپو>
   npm ci
   npm run translate:content -- --limit 500 --batch 4
   ```
   (کلید را یا در `.env.local` بگذار یا مطمئن شو `SUPABASE_URL` و `SUPABASE_SERVICE_ROLE_KEY` ست هستند تا از دیتابیس بخواند.)

3. **خودکار در Vercel:** کرون روزانه `/api/cron/translate` (در `vercel.json`) همهچیز را خودکار انجام میدهد — فقط کلید را در پنل ادمین بگذار.

اگر سهمیه رایگان روز تمام شود، خطا 429 میگیری — اشکالی ندارد، فردا همان دستور را دوباره بزن؛ فقط موارد باقیمانده ترجمه میشوند (اجرا قابل ادامه است).

## ۱۲) تغییر دامنه

دامنه جدید در کل کد اعمال شد (`src/config/site.ts` → fallback جدید، sitemap، canonical، robots، RSS، و User-Agent بات). اگر در Vercel هنوز `NEXT_PUBLIC_SITE_URL` ست شده، آن را هم به دامنه جدید تغییر بده (یا پاک کن تا fallback استفاده شود).

⚠️ **نکته:** املا را دوباره چک کن — `creatorsaicenter.vercel.app` کمی عجیب به نظر میرسد (شاید منظور `creatorsaicenter` یا مشابه باشد). اگر غلط باشد، canonical و sitemap به آدرس اشتباه اشاره میکنند.

---

## ۱۳) ارتقای بخش اخبار (v3) — فقط منابع واقعی و رایگان

بر اساس راهنمای AI News Aggregator (GUIDE.md) بخش اخبار بازسازی شد:

**قبل (بد):** ۶۵ خبر «نمونه» دستنویس که منبعسنجی نشده بودند بهعنوان fallback نمایش داده میشدند + خلاصه AI فقط با OpenAI پولی.

**بعد (خوب):**
- ❌ اخبار نمونه جعلی **کاملاً حذف شدند** — اگر خبر واقعی نباشد، صفحه حالت خالی صادقانه نشان میدهد (نه پر کردن با ساختگی)
- ✅ **۱۲ منبع RSS رایگان و نامحدود** (بدون کلید): The Verge, VentureBeat, TechCrunch, OpenAI, Google DeepMind, Google AI, MIT Technology Review, Ars Technica, Towards Data Science, Hugging Face, Anthropic, The Decoder
- ✅ **خلاصهسازی AI رایگان** (Gemini 3.6 Flash) برای ۲۰ خبر اول هر بار — با کش ۶ ساعته تا سهمیه رایگان هدر نرود
- ✅ گیت ارتباطی (relevance gate) هنوز فعال است: فقط خبرهایی که واقعاً درباره AI برای ویدیو/صدا/سازندگاناند نمایش داده میشوند

**نحوه کار:** صفحه `/news` هر بار (ISR ساعتی) از RSS زنده میخواند، فیلتر میکند و خلاصه رایگان میسازد. کرون روزانه هم برای ذخیره در دیتابیس (در صورت تنظیم Supabase) و ترجمه اخبار جدید فعال است.

## ۱۴) کلید Gemini شما

کلید `GEMINI_API_KEY` که دادی تست و کار کرد ✅ — مدل فعال `gemini-3.6-flash` (مدل قبلی `2.5-flash` برای کاربران جدید بسته شده بود؛ بهروز شد).

- الان در `.env.local` ریپو گذاشته شده (خارج از گیت و زیپ میماند)
- بعد از دیپلوی، همان کلید را در **پنل ادمین → Translation** بگذار (یا بهعنوان `GEMINI_API_KEY` در Vercel env) تا هم کرون و هم GitHub Action از آن استفاده کنند
- ⚠️ چون کلید در چت گذاشته شد، بعداً در AI Studio یک کلید جدید بساز و این یکی را بچرخان

## ۱۵) دامنه

دامنه اصلاح شد: `https://creatorsaicenter.vercel.app` (املا درست شد) — در `src/config/site.ts`، sitemap، canonical، RSS و User-Agent باتها اعمال شد.

---

## ۱۶) اخبار: انتشار خودکار + آرشیو از ابتدای ۲۰۲۶ (v3.1)

دو تغییر درخواستی انجام شد:

**۱) مرحله تأیید حذف شد.** قبلاً اخبار بعد از گرفتن از RSS بهصورت `pending` میماندند تا در پنل ادمین تأیید شوند. حالا هر خبری که از **گیت ارتباطی خودکار** (مربوط به AI برای ویدیو/صدا/سازندگان) عبور کند، **بلافاصله منتشر میشود** (`approved: true`). پنل ادمین → News Queue حالا فقط نمایشی است (بدون دکمه تأیید/رد).

**۲) آرشیو از ابتدای ۲۰۲۶.** دو لایه:
- **RSS**: عمق واکشی زیاد شد (۴۰ خبر از هر منبع، مجموع ۲۰۰) — ولی RSS فقط ~یک هفته اخیر را میدهد
- **GNews API (رایگان)** برای بکفیل واقعی: با گذاشتن کلید رایگان GNews در `GNEWS_API_KEY`، سیستم با فیلتر تاریخ `from=2026-01-01` خبرهای واقعی از ژانویه ۲۰۲۶ به بعد را میگیرد (۴ جستجوی کلیدواژهای). کلید رایگان: https://gnews.io → 100 درخواست/روز.
- بدون GNews، کرون روزانه بهتدریج آرشیو را به سمت ابتدای ۲۰۲۶ پر میکند (هر روز چند خبر جدید اضافه میشود).

> ⚠️ نکته صادقانه: «همه اخبار از ابتدای ۲۰۲۶» فقط با کلید GNews یکجا ممکن است؛ RSS بهتنهایی به عقبتر از چند روز نمیرسد. اخبار جعلی هم همچنان هیچوقت ساخته نمیشوند.

---

## ۱۷) اخبار: متن کامل، نه خلاصه (v3.2)

خلاصهسازی AI حذف شد. حالا:

- **متن کامل مقاله** از خود منبع گرفته و نمایش داده میشود:
  - اگر RSS متن کامل بدهد (مثل VentureBeat)، همان استفاده میشود
  - اگر RSS فقط خلاصه بدهد (TechCrunch, The Verge, The Decoder, Ars...)، سیستم به **URL خود مقاله** میرود و با `@extractus/article-extractor` متن کامل را استخراج میکند (بدون مرورگر، سریع، مناسب serverless)
  - تست واقعی: TechCrunch از ۲۲۶ کاراکتر خلاصه → **۴۱۴۵ کاراکتر متن کامل**
- **هیچ خلاصه AI ساخته نمیشود** — نشان «AI summary» از کارتها و صفحه جزئیات حذف شد
- کارتها همچنان پیشنمایش کوتاه (excerpt فید) را نشان میدهند و صفحه جزئیات متن کامل را
- استخراج فقط هنگام واکشی انجام میشود (نه هر بار رندر) + کش درونحافظهای تا درخواست تکراری به سایت خبری نرود

وابستگی جدید: `@extractus/article-extractor`

---

## ۱۸) ترجمه AI Studio + بخش Site Content (Preview) در پنل ادمین (v3.3)

### ترجمه AI Studio
تمام صفحات AI Studio (hub + ۷ ابزار + upgrade) حالا از دیکشنریها میخوانند:
- hub: قهرمان، دکمهها، بخشها، ۸ کارت ابزار، اصول، CTA
- ابزارها: عنوان، توضیح، eyebrow هر کدام
- فونتها و RTL مثل بقیه سایت

### بخش Site Content در پنل ادمین
تب جدید **Site Content** (آیکون چشم) در `/admin`:
- **Live preview**: iframe از کل سایت (نمایش زنده)
- **ادیتور**: فیلدهای قابل ادیت برای Homepage hero (عنوان/زیرعنوان/دکمهها)، بخش Featured، بخش Newsletter، و AI Studio hero
- **Save & publish**: تغییرات در `site_settings` (کلیدهای `content_*`) ذخیره و **بلافاصله روی سایت اصلی اعمال** میشود
- متنهای ادیتشده override میکنند؛ اگر خالی باشند، ترجمه پیشفرض (دیکشنری) نمایش داده میشود

معماری: `src/lib/siteContent.ts` (خواندن/نوشتن) + `src/app/api/admin/site-content/route.ts` (فقط ادمین، با CSRF) + صفحات خانه و AI Studio آن را میخوانند.

---

## ۱۹) ترجمهٔ کامل UI — همهٔ صفحات + کامپوننتها (v3.4)

### چه چیزی اضافه شد
- **صفحات جدید ترجمهشده**: stack-builder، calculators، benchmark، developers، compare/[pair]، alternatives/[slug]، account، login، privacy، terms، submit، founders، jobs، templates و کل پنل ادمین.
- **کامپوننتهای مشترک**: VerificationBadge، ScoreBreakdown، EvidenceCard، PricingPlansSection، PriceHistorySection، ShareButtons، ReviewSection، HelpfulFeedback، HomeSearch، ToolRecommenderModal، StarkPoll، BenchmarkLeaderboard، TestingQueueWidget و هر ۸ ابزار AI Studio (PromptBuilder، ThumbnailBrief، ThumbnailText، ContentCalendar، ImageTools، SubtitleTools، AudioTrimmer، VideoInspector) + قاب StudioToolChrome و CTAها.
- Namespaceهای جدید در `messages/en.json`:
  `stackBuilder`، `calculators`، `benchmark`، `developers`، `comparePair`، `alternatives`، `account`، `login`، `privacy`، `terms`، `submit`، `founders`، `jobs`، `templates`، `admin`، `studio`، `components.*`

### نکتهٔ مهم: t.rich فقط در کامپوننت کلاینت!
`t.rich()` در کامپوننت **سرور** در این نسخهٔ next-intl خطای
«Functions cannot be passed directly to Client Components» میدهد.
قانون:
- در صفحات سرور → متن را به چند بخش کنار هم (`t('...')` + `<Link>...</Link>`) بچین، نه `t.rich`.
- در کامپوننتهای `'use client'` → `t.rich` اشکالی ندارد (پنل ادمین، فرمها).

### Bootstrap ترجمه با Google Translate (بدون کلید)
وقتی سهمیهٔ رایگان Gemini تمام است، دو اسکریپت بدون کلید کار را جلو میبرند (کیفیت پایه؛ بعداً موتور حرفهای روی همان کلیدها مینشیند و پولیش میکند):
- `node scripts/fill-missing-ui.mjs [locales...]` — هر کلیدِ ناقص را با Google Translate پر میکند.
- `node scripts/fix-placeholders.mjs` — جایدارندههای `{name}` را با توکنهای مات (ZX) حفظ میکند تا ترجمه خرابشان نکند.
- هر دو اسکریپت کلیدهای پر شده را از `messages/.cache` حذف میکنند تا `npm run translate:ui` بعدی (با Gemini/OpenAI/OpenRouter) دوباره با موتور حرفهای ترجمه و کش کند.
- ⚠️ آرایهها (مثل `benchmark.briefs.b1.measures`) نباید به آبجکت عددی تبدیل شوند — `unflatten` هر دو اسکریپت الان آرایهسازی خودکار دارد.

---

## ۲۰) ترجمهٔ خودکار اخبار جدید — ۳ لایه (v3.4)

جواب سؤال «اخبار جدید هم اتومات ترجمه میشه؟» بله، سه لایه دارد:

1. **هنگام ingest** (`/api/news/refresh` و دکمهٔ «Ingest now» در ادمین):
   `autoTranslateNews` عنوان + خلاصهٔ ۸ خبر اول و متن کامل ۳ خبر آخر را به هر ۷ زبان ترجمه و در
   `content_translations` ذخیره میکند (محدود و best-effort تا کرون متوقف نشود).
2. **هنگام بازدید** (جدید در v3.4): وقتی کاربر مقالهای را به زبان غیرانگلیسی باز میکند،
   `src/lib/newsTranslate.ts` → `ensureNewsTranslation` فیلدهای ناقص (عنوان/خلاصه/متن کامل/دسته)
   را همان لحظه ترجمه و کش میکند. اولین بازدیدکنندهٔ هر زبان کمی صبر میکند، بقیه فوری.
   اگر موتور در دسترس نباشد، صفحه با متن انگلیسی رندر میشود (بدون خطا).
3. **کرون روزانه** `/api/cron/translate` + اکشن GitHub: هر چیزی که جا مانده را در بازههای بعدی برمیدارد.

پیشنیاز: provider در پنل ادمین (تب Translation) یا متغیر محیطی تنظیم شده باشد (ترجیحاً Gemini رایگان).

---

## ۲۱) زیرمنوی زبان: یادداشت ترجمه + Google Translate (v3.5)

زیر منوی زبان (بالای صفحه، همهٔ زبانها):
- یادداشت کوچک: «ممکن است برخی بخشها هنوز بهطور کامل ترجمه نشده باشند.»
- دکمهٔ **Translate with Google** → صفحه را در Google Translate باز میکند
  (منبع انگلیسی، مقصد زبان فعلی). بدون کلید و کاملاً رایگان؛ کلیدها در `common`:
  `notAllTranslated` و `googleTranslate`.
- پیادهسازی: `src/components/LanguageSwitcher.tsx` — تابع `googleTranslateUrl`
  پیشوند زبان را از مسیر حذف میکند و URL انگلیسی را به Google میدهد.

---

## ۲۲) پنل ادمین: ویرایش کامل کاتالوگ و بلاگ بدون کدنویسی (v3.5)

### تبهای جدید در /admin
- **ابزارها (Tools catalog)**: لیست همهٔ ابزارها با جستجو + دکمهٔ «Edit» روی هر
  ابزار + دکمهٔ «Add new tool». در فرم ویرایش میتوانی تغییر بدهی: نام، slug،
  تگلاین، آدرس، لوگو، دسته، مدل قیمتگذاری، قیمت شروع، توضیح کوتاه/بلند،
  معرفی صفحه، Best for، تگها، سطح تأیید (verification)، پرچمهای Featured و
  Editor's Choice. ابزارهای ویرایششده برچسب «edited» میگیرند و «Reset to default»
  همهچیز را برمیگرداند.
- **بلاگ (Blog posts)**: لیست پستها + ویرایش (عنوان، slug، خلاصه، دسته، تاریخ،
  زمان مطالعه، ابزار مرتبط، متن کامل markdown) + افزودن پست جدید + Reset.

### چطور بدون دپلوی مجدد اعمال میشود؟
- هر ویرایش بهصورت JSON در جدول `site_settings` ذخیره میشود:
  `catalog_override:{slug}` برای ابزارها، `blog_override:{slug}` برای پستها.
- لایهٔ `src/lib/contentOverrides.ts` این overrideها را هنگام رندر با کاتالوگ
  استاتیک ادغام میکند (کش ۳۰ ثانیهای درون پروسه).
- صفحات کاتالوگ/بلاگ (`/tools`, `/tool/[slug]`, خانه، دستهها، مقایسه،
  جایگزینها، trending, best-of, /blog, /blog/[slug] و sitemap) حالا از
  `getEffectiveTools()` / `getEffectivePosts()` میخوانند و `revalidate = 30`
  دارند؛ ابزارها/پستهای جدید با `dynamicParams = true` بهصورت on-demand رندر
  میشوند. یعنی ذخیره در پنل ≈ تا ۳۰ ثانیه بعد روی سایت زنده است.
- APIها: `/api/admin/tools` و `/api/admin/blog` (GET/PUT/POST/DELETE، فقط ادمین
  با CSRF).
- اگر `SUPABASE_SERVICE_ROLE_KEY` نباشد، هیچچیز نمیشکند — فقط خطای «not
  configured» برمیگردد و سایت از دادههای استاتیک نمایش میدهد.
- سورس تغییرات هر بخش را در پنل میتوانی با دکمهٔ Reset به حالت پیشفرض برگردانی.

### نکتهٔ امنیتی
همان قانون قبل: RLS جدول site_settings از migration 0003، نوشتن فقط از مسیر API
با نشست ادمین. هیچ کلیدی در مرورگر نمایش داده نمیشود.

---

## ۲۳) عکس ابزارها + لینک افیلیت از پنل (v3.5)

### عکس ابزار
ویرایشگر ابزار در /admin (تب «ابزارها» → Edit) حالا برای هر ابزار دو فیلد عکس دارد:
- **Logo** و **Cover image** — هر دو با دکمهٔ **Upload** (آپلود به Supabase Storage، باکس عمومی `tool-images`) یا با چسباندن URL عکس.
- پیشنمایش کوچک کنار فیلد.
- راهاندازی یکباره: باکس `tool-images` را در Supabase → Storage بهصورت public بساز (یا `supabase storage buckets create tool-images --public`). بدون آن، دکمهٔ Upload پیام نرم میدهد و URL عکس همچنان کار میکند.

### لینک افیلیت
بخش جدید **Affiliate link** در ویرایشگر ابزار:
- **Affiliate URL** — لینک با پارامتر/شناسهٔ شبکهات.
- **Affiliate program** — انتخاب شبکه: Impact / PartnerStack / Rewardful / Direct / بدون برنامه.
- قانون صداقت ۱.۶ حفظ شده: سایت فقط وقتی لینک افیلیت میفرستد که شبکه انتخاب شده باشد.
- مسیر `/go/[slug]` حالا از `getEffectiveTool` میخواند → لینک افیلیت ذخیرهشده **تا ~۳۰ ثانیه بعد روی دکمههای Visit زنده است**، بدون دپلوی جدید.
- صفحهٔ `/disclosure` هم ابزارهای دارای برنامهٔ افیلیت را از همان لایه میخواند.
- API آپلود: `POST /api/admin/tools/upload` (multipart، فقط ادمین + CSRF، سقف ۴MB).

---

## ۲۴) فایل اکسل لینکهای افیلیت (v3.5)

فایل تحویلی: `DirectoryAI-Hub-Affiliate-Links.xlsx` (ساخته شد).
- **شیت «ابزارها»**: ۲۰۶ ابزار مرتبشده بر اساس «پتانسیل درآمد» (قیمت شروع بالاتر اول — چون سایت دادهٔ درآمد واقعی ندارد، این یک تخمین شفاف است و قابل مرتبسازی مجدد است). ستونها: ردیف، نام، اسلاگ، دسته، قیمتگذاری، قیمت شروع، وبسایت، لینک فعلی `go/{slug}`، **ستون زرد «لینک افیلیت شما» (خالی برای پر کردن)**، شبکهٔ افیلیت، پتانسیل ۱–۵.
- **شیت «راهنما»**: لینکهای بخش افیلیت سایت (`/deals`, `/disclosure`, الگوی `/go/{slug}`) + راهنمای گامبهگام.

---

## ۲۵) راهاندازی باکس tool-images (آپلود عکس ابزارها)

یکبار باید باکس عمومی `tool-images` در Supabase ساخته شود. سه راه:

**۱) داشبورد (پیشنهادی):** Supabase → پروژه → Storage → New bucket
→ Name: `tool-images` → تیک **Public** → Create.

**۲) SQL (یک خط):** SQL Editor → Run:
```sql
insert into storage.buckets (id, name, public)
values ('tool-images', 'tool-images', true)
on conflict (id) do nothing;
```

**۳) CLI:** `supabase storage buckets create tool-images --public`

فایل آماده: `supabase/migrations/0015_tool_images_bucket.sql`

نکات:
- آپلود فقط از مسیر ادمین (service_role + CSRF) انجام میشود → نیازی به Policy اضافه نیست، کاربران عادی نمیتوانند بنویسند.
- سقف هر فایل ۴MB (JPG/PNG/WebP/SVG/GIF).
- اگر باکس ساخته نشده باشد، دکمهٔ Upload پیام واضح «Bucket does not exist…» میدهد.

---

## ۲۶) عکس و ویدیو ابزارها — رفع «پنل اوکیه ولی سایت اعمال نمیشه» (v3.5)

### مشکل چه بود
فیلد `coverImage` در پنل ذخیره میشد ولی **هیچ صفحهای آن را رندر نمیکرد** — کارت ابزار و هیروی صفحهٔ ابزار فقط CoverArt تولیدشده (گرادیان + حروف) را نشان میدادند. بهعلاوه، صفحات استاتیک کش میشدند و تا دپلوی بعدی تغییر را نشان نمیدادند.

### اصلاحات
- **کارت ابزار**: اگر `coverImage` ست شده باشد، بهجای CoverArt همان عکس نمایش داده میشود (با fallback در صورت خطا). صفحهٔ ابزار (هیرو) هم همین.
- **انتشار فوری**: بعد از هر ذخیره/حذف در پنل، `revalidatePath('/', 'layout')` صدا زده میشود → کش ISR کل سایت همان لحظه پاک میشود، بدون انتظار ۳۰ ثانیه و بدون دپلوی جدید.
- **ویدیو-روی-هاور (مثل GIF)**: فیلد جدید `previewVideoUrl`:
  - در پنل (ابزارها → Edit → بخش «Preview video») میتوانی URL مستقیم .mp4/.webm بگذاری یا با دکمهٔ **Upload video** آپلود کنی.
  - در سایت، وقتی موس روی عکس/کاور ابزار برود، ویدیو بهصورت muted + loop پخش میشود و با خروج موس متوقف میشود.
  - روی کارت ابزار و هیروی صفحهٔ ابزار هر دو کار میکند. اگر کاربر «کاهش حرکت» (reduced motion) فعال داشته باشد، پخش نمیشود.
  - آپلود ویدیو هم از همان مسیر `POST /api/admin/tools/upload` (تا ۲۵MB) — توجه: Vercel Hobby بدنهٔ درخواست را ~۴.۵MB محدود میکند، پس ویدیوی بزرگتر را URL بگذار (مثلاً از Supabase Storage یا CDN).

---

## ۲۷) اکسل افیلیت — PartnerStack بالای لیست (v3.5)

`DirectoryAI-Hub-Affiliate-Links.xlsx` دوباره ساخته شد با:
- ستون جدید **«شبکه»** (PartnerStack / Impact / Rewardful / FirstPromoter / خالی)
- **ابزارهای PartnerStack بالای لیست** (سبز): Descript · ElevenLabs · Murf.ai · Synthesia
- بقیه بر اساس «پتانسیل درآمد» (قیمت شروع بالاتر اول) — چون سایت دادهٔ درآمد واقعی ندارد، تخمین شفاف است
- ستون زرد «لینک افیلیت شما» خالی برای پر کردن + فیلتر روی سرستونها
- تولید مجدد: `npx tsx scripts/dump-tools.ts > /tmp/tools.json && python3 scripts/make-affiliate-xlsx.py /tmp/tools.json`
- ⚠️ ستون «شبکه» بر اساس منابع عمومی است؛ قبل از ثبت، تأیید کن (شبکهها عوض میشوند)

---

## ۲۸) ارتقای بیشتر پنل ادمین (v3.5)

اضافه شد:
- **تب «Deals & coupons»**: افزودن/ویرایش/حذف کد تخفیف و پیشنهاد واقعی (فقط تأییدشده). در صفحهٔ /deals بالای صفحه نمایش داده میشود. ذخیره در `site_settings` کلید `deals_list`، زنده بدون دپلوی جدید. API: `/api/admin/deals`.
- **دکمهٔ Export CSV** در تبهای ابزارها و بلاگ (دانلود فوری لیست با همهٔ فیلدها برای کار در اکسل).
- **نوار کاملبودن** در Overview: چند ابزار لوگو دارند / کاور / لینک افیلیت / ویدیو / ویرایششده — تا بدانی چه چیزی کم است.
- **دکمهٔ «Translate now»** در تب Translation: همان sync ترجمه را همین الان اجرا میکند (بدون نیاز به cron/GitHub). مسیر: `/api/admin/translate/run`.
- **ویدیو در پنل**: پیشنمایش ویدیو داخل ویرایشگر + هشدار اگر لینک یوتیوب/وایمو باشد + نکتهٔ 16:9 برای عکسها.

---

## ۲۹) ویدیو-روی-هاور + 16:9 (v3.5)

- صفحهٔ ابزار (هیرو) حالا **نسبت 16:9** دارد (`aspect-[16/9]` با سقف ارتفاع) — کارت ابزار هم قبلاً 16:9 بود.
- ویدیو روی کارت و هیرو: `preload="metadata"` + `onError` (اگر منبع خراب باشد مخفی میشود) — اگر ویدیو نمیآید، محتملترین دلایل:
  ۱) دپلوی جدید نرفته، ۲) URL مستقیم فایل نیست (یوتیوب/وایمو کار نمیکند)، ۳) فایل از Upload ارسال شده و Vercel بدنهٔ >~۴.۵MB را رد کرده (URL بگذار)، ۴) `tool-images` باکس ساخته نشده.

---

## ۳۰) Node.js 24 (v3.5.1)

Vercel از ۲۰۲۶-۱۰-۰۱ دیپلویهای Node 20 را قبول نمیکند. برای سازگاری:
- `package.json` → `"engines": { "node": "24.x" }`
- `.nvmrc` → `24`
- workflow های GitHub هم از `.nvmrc` میخوانند (نیازی به تغییر ندارند).

اگر در لاگ Vercel دیدی:
`Warning: Due to "engines": { "node": "20.x" } ... Node.js Version "24.x" will not apply`
یعنی پکیج هنوز 20 است — بعد از این تغییر، Node 24 استفاده میشود و هشدار/خطا حذف میشود.

---

## ۳۱) ویدیو پخش نمیشد — دو باگ + راهنمای فایل (v3.5.2)

### چرا پخش نمیشد (دو دلیل پیدا و رفع شد)
۱) **هیروی صفحهٔ ابزار**: ویدیو `pointer-events-none` داشت و با `hover:` روی خودش نمایش داده میشد — چون pointer-events خاموش است، hover هرگز فعال نمیشد و ویدیو نامرئی میماند. حالا `group` روی کانتینر و `group-hover:` روی ویدیو است + تصویر با hover محو میشود.
۲) **فایل mp4 faststart نبود**: در فایل تو، moov (اطلاعات شروع پخش) در انتهای فایل بود. مرورگر با `preload="metadata"` فقط اول فایل را میخواند → هرگز شروع نمیشد. کد حالا در hover `preload='auto' + load() + play()` صدا میزند تا حتی فایلهای غیر-faststart هم شروع شوند.

### فایل اصلاحشده
`opusclip-preview-faststart.mp4` (۲.۸MB، همان کیفیت) با `-movflags +faststart` ساخته شد (moov به اول منتقل شد).
**کاربر باید این را دوباره آپلود کند** (پنل → opusclip → Upload video یا مستقیم در Supabase Storage) و URL جدید را در فیلد Preview video بگذارد.

### چک سریع هر فایل mp4 (اگر باز هم پخش نشد)
- فایل را دانلود کن و بررسی کن moov قبل از mdat است:
  `curl -s URL -o v.mp4 && python3 -c "d=open('v.mp4','rb').read(); print(d.find(b'ftyp'), d.find(b'moov'), d.find(b'mdat'))"`
  اگر moov > mdat → faststart نیست → با ffmpeg اصلاح کن: `ffmpeg -i v.mp4 -c copy -movflags +faststart out.mp4`

### اصلاحات اضافهشده
- `revalidate = 30` به `/tools` و `/compare` اضافه شد (قبلاً نداشتند → کارتها بعد از ذخیرهٔ پنل آپدیت نمیشدند)
- نشان «hover to preview» روی کارت و هیرو وقتی ویدیو دارد

---

## ۳۲) آپلود دستهای ویدیوها (برای صدها ویدیو) (v3.5.3)

دیگر لازم نیست ویدیوها را یکییکی در پنل آپلود کنی. اسکریپت:
- همهٔ فایلهای یک پوشه را میگیرد
- اگر mp4 faststart نباشد، خودش با ffmpeg درست میکند (بدون افت کیفیت)
- به Supabase Storage (`tool-images/{slug}/...`) میفرستد
- **URL را مستقیم در `site_settings` ثبت میکند** → سایت فوراً ویدیو را نشان میدهد (بدون ورود به پنل برای هر ابزار)

### راهاندازی (ویندوز — خیلی ساده)
۱) پوشهٔ `videos` کنار پروژه بساز و ویدیوها را داخلش بگذار، **اسم هر فایل = slug ابزار**:
   - `opusclip.mp4`، `elevenlabs.mp4`، `heygen.mp4` …
   - (slug از آدرس ابزار: مثلاً `creatorsaicenter.vercel.app/tool/opusclip` → `opusclip`)
۲) مطمئن شو `.env.local` اینها را دارد:
   - `SUPABASE_URL=...`
   - `SUPABASE_SERVICE_ROLE_KEY=...`
۳) `upload-videos.bat` را اجرا کن (یا):
   `node scripts/bulk-upload-videos.mjs --dir videos`

### گزینهها
- `--dry-run` → فقط ببین چه کار میکند بدون آپلود
- `--skip-faststart` → اگر مطمئنی فایلها faststart هستند
- `--no-register` → آپلود کن ولی در سایت ثبت نکن

### ffmpeg
برای faststart لازم است (اگر نصب نباشد، اسکریپت پیام میدهد). دانلود: https://www.gyan.dev/ffmpeg/builds/ یا فقط `ffmpeg` را نصب کن. اگر FFMPEG_BIN را ست کنی هم استفاده میشود.
