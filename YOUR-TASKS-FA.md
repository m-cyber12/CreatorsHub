# ✅ لیست کارهای تو — قدم‌به‌قدم و بدون ابهام (نسخه ۲، ۱۲ اوت)

ترتیب مهم است: اول ۱ (کد)، بعد ۲ تا ۶، آخر ۷ و ۸. مجموعاً ~۴۵ دقیقه.

---

## مرحله ۱ — اعمال کد جدید روی گیت‌هاب (~۵ دقیقه)

> ⭐ **آپدیت عصر:** دیدم تو دسته اول فیکس‌ها را دیپلوی کردی (`feat: 27` روی گیت‌هاب و Vercel). پچ جدید دقیقاً **بر پایه همان کامیت** ساخته شده و فقط اصلاحات امروز عصر را دارد (لاگین صادقانه + ارورهای فارسی + فیکس‌های t.rich + سازگاری URL سفارش‌ها). یعنی تداخلی با تغییرات تو ندارد.

**راه A (پچ — پیشنهادی، چون دسته اول را خودت کامیت کردی):** `creatorai-fixes-2026-08-12.patch` را کنار ریپوی لوکالت بگذار:
```bash
git checkout main && git pull          # باید روی feat: 27 باشی
git apply creatorai-fixes-2026-08-12.patch   # تست کردم — روی feat: 27 تمیز apply می‌شود ✅
git add -A && git commit -m "fix: honest signup flow + localized auth errors + t.rich fixes" && git push
```

**راه B (زیپ — جایگزینی کامل):** پوشه `creatorshub` داخل `creatorai-hub-fixed-2026-08-12.zip` نسخه کامل و نهایی است؛ اگر راه A گیر کرد، محتویاتش را جایگزین پوشه پروژه کن و `git add -A && git commit && git push`.

**تأیید:** بعد از پوش، Vercel خودکار دیپلوی می‌کند؛ صبر کن سبز شود. بعد برو به `/fa/login` — دیگر نباید دکمه «Continue to Dashboard» را بعد از ثبت‌نام ببینی (الان روی سایت live هست و همان باگی بود که گزارش دادی).

---

## مرحله ۲ — کلید پلیسیو ✔ (احتمالاً انجام شده — فقط چک کن)

کلیدی که در گیت‌هاب لو رفته بود الان از نظر API پلیسیو **401 (نامعتبر)** برمی‌گردد — یعنی یا خودت عوضش کردی یا از قبل خاص بود. در داشبورد پلیسیو (plisio.net → API) مطمئن شو کلید فعلی با آن کلید قدیمی فرق دارد. بعد مقدارش را در Vercel بگذار (مرحله ۴).

**Callback URL** در تنظیمات فروشگاه پلیسیو:
`https://creatorsaicenter.vercel.app/api/checkout/plisio-webhook`

---

## مرحله ۳ — تنظیمات Supabase (برای درست شدن کامل ورود/ثبت‌نام) (~۵ دقیقه)

باگ «ثبت‌نام می‌کنم ولی ورود میگه رمز اشتباهه» در کد فیکس شد، ولی برای تجربه کامل این‌ها را هم در Supabase تنظیم کن (supabase.com → پروژه‌ات):

1. **Authentication → URL Configuration:**
   - **Site URL:** `https://creatorsaicenter.vercel.app`
   - **Redirect URLs** (Add): `https://creatorsaicenter.vercel.app/**`
   - دلیل: لینک تأیید ایمیل بعد از کلیک به این آدرس‌ها برمی‌گردد؛ اگر تنظیم نباشد به localhost می‌رود و کاربر گیر می‌کند.
2. **Authentication → Sign In / Providers → Email:**
   - **Confirm email = ON بماند** (پیشنهاد من). کد جدید با هر دو حالت درست کار می‌کند؛ با ON فقط صاحب واقعی ایمیل حساب می‌سازد.
3. **SMTP اختصاصی = اجباری برای لانچ** ⚠️ SMTP پیش‌فرض Supabase فقط **چند ایمیل در ساعت** اجازه می‌دهد — در تست‌های امروز من چند بار به سقف (`over_email_send_rate_limit`) خوردم؛ یعنی با اولین موج کاربر واقعی، ایمیل‌های تأیید fail می‌شوند. راه‌حل: **Authentication → SMTP Settings** → **Resend** (resend.com، رایگان تا ۱۰۰ ایمیل/روز، ۵ دقیقه کار دارد: مسیر Docs → Supabase → Send with Resend). وقتی SMTP سفارشی وصل شود سقف برمی‌دارد. (تا آن موقع، کاربر حداقل پیام فارسی «چند دقیقه صبر کن» می‌بیند و دکمه «ارسال دوباره» هست.)

## مرحله ۴ — متغیرهای Vercel (~۵ دقیقه)

Vercel → پروژه → **Settings → Environment Variables** (هر ۳ محیط تیک بخورد):

| Name | Value |
|---|---|
| `NEXT_PUBLIC_SITE_URL` | `https://creatorsaicenter.vercel.app` ← **الان روی دامنه قدیمی اشتباه است!** |
| `PLISIO_API_KEY` | کلید فعلی پلیسیو |
| `ADMIN_SESSION_SECRET` | ۶۴ کاراکتر hex رندوم (`openssl rand -hex 32`) |

بعد: **Deployments → آخرین مورد → ⋯ → Redeploy** (بدون Redeploy، env جدید اعمال نمی‌شود).

> اختیاری ولی خوب: `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` از upstash.com (رایگان) → ریت‌لیمیت جهانی واقعی.

## مرحله ۵ — مایگریشن Supabase (~۳ دقیقه)

Supabase → **SQL Editor** → New query → کل محتوای `supabase/migrations/0017_ai_usage_daily.sql` را Paste → **Run**.
تأیید: **Table Editor** → جدول `ai_usage_daily` دیده شود.

## مرحله ۶ — چک نهایی بعد از دیپلوی (~۵ دقیقه)

- صفحه ابزار → View Source → `canonical` باید `creatorsaicenter.vercel.app` باشد نه directory-ai-hub
- کنسول مرورگر (F12) در صفحه اصلی: دیگر خطای `Content Security Policy` برای ویدیو/یوتیوب نباید باشد
- `/fa`: تیتر فارسی · `/login` فارسی: تب‌ها و دکمه‌ها فارسی
- **تست ثبت‌نام واقعی:** با جیمیل واقعی خودت Sign Up کن → پیام «لینک تأیید ارسال شد» → ایمیل را بزن → بعد Sign In باید کار کند. (بدون کلیک روی لینک ایمیل: پیام صادقانه «ایمیلت هنوز تأیید نشده» + دکمه ارسال دوباره، نه «رمز اشتباه»)
- **تست وبهوک جعلی (اختیاری):**
```bash
curl -X POST https://creatorsaicenter.vercel.app/api/checkout/plisio-webhook \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "order_number=ord_test123&status=completed"
```
باید جواب بدهد: `{"error":"Invalid signature"}` و وضعیت ۴۰۳ ✔ (قبلاً با همین تست، سفارش تأیید می‌شد!)

## مرحله ۷ — پاک‌سازی داده‌های تست من

پنل `/admin`:
- Reviews → ریویوی «AuditBot» روی OpusClip → حذف
- Subscribers → `probe-audit-123@example.com` → حذف
- سفارش‌های تستی امروز در Orders (اگر دیده می‌شوند): چند `ord_1786533…` با نام «AuditSandbox Video Tool» / «Warm» / «AuditSandbox Free Tool» → حذف (یا به‌جای حذف، یکی را برای تست جریان تأیید دستی دست نگه دار)

داشبورد Supabase:
- **Authentication → Users** → تست‌یوزرها: `audit2-1786529788@gmail.com` و `audit3-1786534069792@gmail.com` (تأییدنشده) → حذف

## مرحله ۸ — دامنه قدیمی + دامنه اختصاصی (قبل از لانچ)

- `directory-ai-hub.vercel.app` را در Vercel → Domains به دامنه جدید/فعلی 301 کن
- راهنمای خرید دامنه: `DOMAIN-GUIDE-FA.md` (خلاصه: Porkbun/Cloudflare، ~۱۰-۱۲$/سال، .com)

---

## 💳 درباره تست درگاه (پاسخ به سؤالت)

بدون جابه‌جایی ارز، هر چیزی که قابل تست بود را تست کردم:

| تست | نتیجه |
|---|---|
| امضای صحیح/غلط وبهوک (ساختگی + replay + tamper) | ۹ تست واحد سبز ✔ |
| سفارش واقعی ساخته شود و وبهوک جعلی بزنم (روی بیلد واقعی) | **403 رد شد** ✔ |
| وبهوک با امضای صحیح ولی تراکنش ناموجود | **202 → سفارش pending می‌ماند** (fail-safe) ✔ |
| پارسر پاسخ operations پلیسیو (هر دو فرم لیست) | تست شد ✔ |
| سناریوی دوبار خرج کردن یک تراکنش | مسدود شد (order_number باید مچ باشد) ✔ |
| کلید لیک‌شده قبلی | **401 — مرده است** ✔ |
| جریان کامل چک‌اوت در مرورگر واقعی (Submit Tool → پلن ۲۹$ → مودال پرداخت) | ۹/۹ ✔ — سفارش ساخته شد، کیف پول+QR+کپی+فرم TXID سالم، بدون کلید پلیسیو gracefully روی حالت دستی افتاد |
| TXID جعلی (`deadbeef…`) در فرم Verify | فقط وضعیت `submitted` شد — **هرگز `paid` نشد** ✔ |

تنها چیزی که بدون پول واقعی قابل اثبات نیست: پاسخِ callback واقعی پلیسیو دقیقاً همین سریال امضا را بزند؛ برای همین کد را طوری نوشتم که **اثبات نهایی همیشه از lookup سرور‌به‌سرور** باشد نه فقط امضا — یعنی حتی اگر سریال امضا با حدس من فرق کند، پرداخت واقعی گم نمی‌شود (تأیید از راه دوم انجام می‌شود). هر وقت اولین خرید واقعی اتفاق افتاد (یا حسابت شارژ شد و ۱ دلار تستی زدی)، اگر سفارش pending ماند، لاگ فانکشن `plisio-webhook` در Vercel را برایم بفرست تا دقیقش کنم.

### گیر کردی؟
شماره مرحله + متن/اسکرین خطا + آخرین کاری که کردی رو بفرست.
